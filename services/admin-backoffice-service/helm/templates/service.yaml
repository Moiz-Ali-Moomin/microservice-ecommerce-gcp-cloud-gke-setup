apiVersion: v1
kind: Service
metadata:
  name: { { include "admin-backoffice-service.fullname" . } }
  labels: { { - include "admin-backoffice-service.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector:
    { { - include "admin-backoffice-service.selectorLabels" . | nindent 4 } }
