apiVersion: v1
kind: Service
metadata:
  name: {{ include "redirect-service.fullname" . }}
  labels:
    {{- include "redirect-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "redirect-service.selectorLabels" . | nindent 4 }}
