apiVersion: v1
kind: Service
metadata:
  name: {{ include "storefront-service.fullname" . }}
  labels:
    {{- include "storefront-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "storefront-service.selectorLabels" . | nindent 4 }}
