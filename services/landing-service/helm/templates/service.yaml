apiVersion: v1
kind: Service
metadata:
  name: {{ include "landing-service.fullname" . }}
  labels:
    {{- include "landing-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "landing-service.selectorLabels" . | nindent 4 }}
