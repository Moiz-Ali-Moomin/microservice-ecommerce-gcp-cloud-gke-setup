replicaCount: 1
image:
  repository: registry.gitlab.com/haxor/ecommerce-platform/attribution-service
  tag: latest
service:
  type: ClusterIP # Not exposed, background worker
  port: 8080 # Health check
env:
  KAFKA_BROKERS: "kafka-0.kafka-headless.kafka.svc:9092"
resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi
